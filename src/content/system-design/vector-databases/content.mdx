# Vector Databases

A vector database stores learned representations and retrieves items whose vectors are close to a query vector. Its job is to make that similarity operation fast, filterable, fresh, and dependable at a scale where comparing every vector is impossible.

This article progresses from first principles to research questions. At every level, trace a request, state assumptions, quantify tradeoffs, and preserve a simple baseline. The recurring design pressures are recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set.

## Beginner: Foundations

:::note
**Prerequisites:** **Linear algebra: vectors, dot product, cosine similarity**, **Basic Python**, **What a database index is (conceptually)**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **build the vocabulary and trace one request end to end**. A vector database stores learned representations and retrieves items whose vectors are close to a query vector. Its job is to make that similarity operation fast, filterable, fresh, and dependable at a scale where comparing every vector is impossible. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Explain what a vector embedding is and why similarity search requires a different data model than SQL

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **embedding API** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Describe the fundamental difference between exact nearest-neighbor and approximate nearest-neighbor search

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **write log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Name the 3 dominant indexing families (HNSW, IVF, LSH) and what problem each solves

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. The capability described by this objective is practical, not a definition to memorize. Begin at the **vector store** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Set up a Qdrant/Pinecone/Weaviate instance and perform a basic semantic search query

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ANN index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: Embedding pipeline: text → tokenizer → transformer → pooling → vector

```text
Vector Databases: Embedding pipeline text tokenizer transformer pooling vector

[text]
        |
        v
  [tokenizer]
          |
          v
    [transformer]
            |
            v
      [pooling]
              |
              v
        [vector]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Embedding pipeline: text → tokenizer → transformer → pooling → vector** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 2: Vector database anatomy: index layer / storage layer / query layer

```text
Vector Databases: Vector database anatomy index layer storage layer query layer

  [producer / request]
           |
           v
      [write log] ---- control, version, policy ----> [vector store]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [ANN index] ---- measured result + trace -----> [metadata index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Vector database anatomy: index layer / storage layer / query layer** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 3: ANN search path: query vector → index traversal → distance computation → k-NN result

```text
Vector Databases: ANN search path query vector index traversal distance computation k NN result

[query vector]
        |
        v
  [index traversal]
          |
          v
    [distance computation]
            |
            v
      [k-NN result]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **ANN search path: query vector → index traversal → distance computation → k-NN result** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 4: HNSW graph visualization: layers, entry point, greedy traversal

```text
Vector Databases: HNSW graph visualization layers entry point greedy traversal

  [producer / request]
           |
           v
      [ANN index] ---- control, version, policy ----> [metadata index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [query coordinator] ---- measured result + trace -----> [reranker]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **HNSW graph visualization: layers, entry point, greedy traversal** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

### Case Studies

#### Spotify: how song embeddings power "Recommended for You"

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Spotify: how song embeddings power "Recommended for You"**—creates pressure at the **embedding API** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Stack Overflow: semantic search over 50M questions without keyword matching

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Stack Overflow: semantic search over 50M questions without keyword matching**—creates pressure at the **write log** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Notion AI: embedding-based document retrieval for in-product search

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Notion AI: embedding-based document retrieval for in-product search**—creates pressure at the **vector store** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: What is the curse of dimensionality and how does it affect nearest-neighbor search?

As dimensions grow, distance values concentrate and tree partitions lose pruning power; many points look similarly far away. Exact search therefore touches too much of the collection, while ANN uses navigable graphs, coarse partitions, or compressed codes to inspect a promising subset and measures the resulting recall loss. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: Why can't you use a B-tree index for vector search?

A B-tree orders scalar keys and prunes by one-dimensional intervals. Vector similarity is a high-dimensional distance relation with no single total ordering that preserves neighborhoods, so a B-tree cannot efficiently discard most candidates. Use an ANN index and a separate metadata index for filters. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: Explain HNSW to a non-technical interviewer in 90 seconds

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: When would you choose L2 distance over cosine similarity?

Choose L2 when vector magnitude carries meaning or the training objective used Euclidean geometry. Choose cosine when direction matters and magnitude should be ignored. For normalized vectors, ranking by cosine and squared L2 is equivalent, so operational convenience can decide. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Intermediate: Design Deep Dive

:::note
**Prerequisites:** **Beginner module complete**, **Understanding of B-trees and LSM trees**, **Basic distributed systems: replication, sharding**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **make design choices and quantify their tradeoffs**. A vector database stores learned representations and retrieves items whose vectors are close to a query vector. Its job is to make that similarity operation fast, filterable, fresh, and dependable at a scale where comparing every vector is impossible. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Design a vector database from requirements: choose indexing strategy given latency, recall, and memory constraints

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **embedding API** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Explain HNSW construction and search in detail (M, ef_construction, ef_search parameters)

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. The capability described by this objective is practical, not a definition to memorize. Begin at the **write log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Design a filtered vector search system (metadata + vector search combined)

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **vector store** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Reason about the recall-latency tradeoff and how to tune it

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ANN index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 5: Choose between a dedicated vector DB and pgvector for a given use case

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **metadata index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: HNSW multi-layer graph with construction algorithm (insert node at each layer)

```text
Vector Databases: HNSW multi layer graph with construction algorithm insert node at each layer

  [producer / request]
           |
           v
      [embedding API] ---- control, version, policy ----> [write log]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [vector store] ---- measured result + trace -----> [ANN index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **HNSW multi-layer graph with construction algorithm (insert node at each layer)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 2: IVF index: K-means cluster centroids + inverted lists + PQ compression

```text
Vector Databases: IVF index K means cluster centroids inverted lists PQ compression

  [producer / request]
           |
           v
      [write log] ---- control, version, policy ----> [vector store]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [ANN index] ---- measured result + trace -----> [metadata index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **IVF index: K-means cluster centroids + inverted lists + PQ compression** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 3: Filtered search: pre-filter vs post-filter vs in-filter (HNSW with payload index)

```text
Vector Databases: Filtered search pre filter vs post filter vs in filter HNSW with payload index

  [producer / request]
           |
           v
      [vector store] ---- control, version, policy ----> [ANN index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [metadata index] ---- measured result + trace -----> [query coordinator]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Filtered search: pre-filter vs post-filter vs in-filter (HNSW with payload index)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 4: Vector DB horizontal sharding: consistent hashing, replica sets, coordinator routing

```text
Vector Databases: Vector DB horizontal sharding consistent hashing replica sets coordinator routing

  [producer / request]
           |
           v
      [ANN index] ---- control, version, policy ----> [metadata index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [query coordinator] ---- measured result + trace -----> [reranker]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Vector DB horizontal sharding: consistent hashing, replica sets, coordinator routing** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 5: Hybrid search: dense vector score + BM25 score → RRF fusion → reranked result

```text
Vector Databases: Hybrid search dense vector score BM25 score RRF fusion reranked result

[dense vector score + BM25 score]
        |
        v
  [RRF fusion]
          |
          v
    [reranked result]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Hybrid search: dense vector score + BM25 score → RRF fusion → reranked result** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

### Case Studies

#### Airbnb: how they combine listing embeddings with metadata filters (price range, location) — why post-filter fails at scale and why they moved to HNSW-native filtering

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Airbnb: how they combine listing embeddings with metadata filters (price range, location) — why post-filter fails at scale and why they moved to HNSW-native filtering**—creates pressure at the **embedding API** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### LinkedIn: Skills graph — embedding 900M member profiles, handling real-time updates to embeddings on profile edit

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**LinkedIn: Skills graph — embedding 900M member profiles, handling real-time updates to embeddings on profile edit**—creates pressure at the **write log** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Elastic (Elasticsearch): migration story from BM25-only to hybrid dense+sparse search

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Elastic (Elasticsearch): migration story from BM25-only to hybrid dense+sparse search**—creates pressure at the **vector store** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: You have 1B vectors, 128 dimensions. Memory budget is 32GB. Walk me through your indexing strategy.

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: A customer complains that semantic search returns irrelevant results when they add a price filter. What's happening and how do you fix it?

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: Compare HNSW and IVF+PQ. When do you choose each?

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: How does product quantization work? What is the recall cost?

Quantization stores weights or activations with fewer bits, reducing memory traffic and allowing larger models or batches. The benefit depends on hardware kernels and calibration; always measure quality by task and slice rather than trusting bit width alone. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q5: Design vector search that supports real-time inserts at 10K/sec without taking the index offline.

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Advanced: Production at Scale

:::note
**Prerequisites:** **Intermediate module complete**, **Distributed systems: consensus, replication, CAP theorem**, **Production infra: Kubernetes, observability basics**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **operate the system under scale, failure, security, and cost pressure**. A vector database stores learned representations and retrieves items whose vectors are close to a query vector. Its job is to make that similarity operation fast, filterable, fresh, and dependable at a scale where comparing every vector is impossible. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Design a distributed vector database serving 100M QPS with 10ms p99 latency

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **embedding API** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Implement index compression strategies: PQ, SQ, binary quantization — and reason about quality/memory tradeoffs

Quantization stores weights or activations with fewer bits, reducing memory traffic and allowing larger models or batches. The benefit depends on hardware kernels and calibration; always measure quality by task and slice rather than trusting bit width alone. The capability described by this objective is practical, not a definition to memorize. Begin at the **write log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Design multi-tenancy: per-tenant index isolation vs shared indexes with namespace filtering

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **vector store** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Build a vector DB observability stack: recall monitoring, latency percentiles, drift detection

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ANN index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 5: Handle embedding model updates without full re-indexing (incremental migration strategies)

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **metadata index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: Full production vector DB cluster: write path (primary + replicas + WAL) + read path (coordinator + shards + cache)

```text
Vector Databases: Full production vector DB cluster write path primary replicas WAL read path coordinator shards cache

  [producer / request]
           |
           v
      [embedding API] ---- control, version, policy ----> [write log]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [vector store] ---- measured result + trace -----> [ANN index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Full production vector DB cluster: write path (primary + replicas + WAL) + read path (coordinator + shards + cache)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 2: Tiered index storage: hot (HNSW in DRAM) → warm (PQ-compressed on SSD) → cold (flat index on object storage)

```text
Vector Databases: Tiered index storage hot HNSW in DRAM warm PQ compressed on SSD cold flat index on object storage

[hot (HNSW in DRAM)]
        |
        v
  [warm (PQ-compressed on SSD)]
          |
          v
    [cold (flat index on object storage)]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Tiered index storage: hot (HNSW in DRAM) → warm (PQ-compressed on SSD) → cold (flat index on object storage)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 3: Multi-tenancy: tenant-per-collection vs namespace-per-tenant vs shared index with payload filtering — memory and latency profile of each

```text
Vector Databases: Multi tenancy tenant per collection vs namespace per tenant vs shared index with payload filtering memory and latency profile of each

  [producer / request]
           |
           v
      [vector store] ---- control, version, policy ----> [ANN index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [metadata index] ---- measured result + trace -----> [query coordinator]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Multi-tenancy: tenant-per-collection vs namespace-per-tenant vs shared index with payload filtering — memory and latency profile of each** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 4: Embedding pipeline at scale: batch embedding service → queue → index update worker → version-stamped index

```text
Vector Databases: Embedding pipeline at scale batch embedding service queue index update worker version stamped index

[batch embedding service]
        |
        v
  [queue]
          |
          v
    [index update worker]
            |
            v
      [version-stamped index]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Embedding pipeline at scale: batch embedding service → queue → index update worker → version-stamped index** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 5: Recall monitoring dashboard: ground truth sampling, approximate recall@K tracking over time

```text
Vector Databases: Recall monitoring dashboard ground truth sampling approximate recall K tracking over time

  [producer / request]
           |
           v
      [metadata index] ---- control, version, policy ----> [query coordinator]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [reranker] ---- measured result + trace -----> [embedding API]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Recall monitoring dashboard: ground truth sampling, approximate recall@K tracking over time** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

### Case Studies

#### Pinecone: architecture of their serverless tier — how they achieve sub-10ms latency without pinning HNSW in RAM (lazy loading + caching strategy)

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Pinecone: architecture of their serverless tier — how they achieve sub-10ms latency without pinning HNSW in RAM (lazy loading + caching strategy)**—creates pressure at the **embedding API** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Weaviate: multi-tenancy architecture — how they isolated 10K tenants without proportional memory overhead

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Weaviate: multi-tenancy architecture — how they isolated 10K tenants without proportional memory overhead**—creates pressure at the **write log** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Meta FAISS at scale: how Instagram uses FAISS for 500M photo embeddings — sharding strategy, replication, the GPU index tier

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Meta FAISS at scale: how Instagram uses FAISS for 500M photo embeddings — sharding strategy, replication, the GPU index tier**—creates pressure at the **vector store** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: Design a vector database for a legal discovery system: 50B documents, queries must return results in under 100ms, recall must be above 95%. Walk through every layer of the architecture.

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: How do you handle embedding model drift? The new model produces incompatible vector spaces.

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: Your vector DB is at 90% memory utilization. What are your options? Walk through the tradeoffs of each.

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: How do you test recall in production without ground truth labels?

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q5: A tenant's index has 500M vectors and HNSW construction is taking 6 hours. What do you change?

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. For **Vector Databases**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Research: Frontiers

:::note
**Prerequisites:** **Advanced module complete**, **Read: HNSW (Malkov 2018), FAISS (Johnson 2019), DiskANN (Microsoft 2019), SPFresh (2022), CAPS (2023)**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **identify open assumptions, evaluate frontier ideas, and design falsifiable experiments**. A vector database stores learned representations and retrieves items whose vectors are close to a query vector. Its job is to make that similarity operation fast, filterable, fresh, and dependable at a scale where comparing every vector is impossible. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Analyze the academic frontier: learned indexes for ANN search, GPU-resident HNSW, hybrid sparse-dense indexes

HNSW is a layered proximity graph. Search enters a sparse upper layer, greedily approaches the query, then descends into denser layers and explores a bounded candidate queue. Parameters such as M and ef trade memory and construction cost for recall and latency. The capability described by this objective is practical, not a definition to memorize. Begin at the **embedding API** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Critique the latency vs recall Pareto frontier and identify where current algorithms fall short

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **write log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Design a novel indexing strategy for a specific hard constraint (streaming updates, extreme dimensionality, binary vectors)

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **vector store** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Propose a research contribution at the intersection of vector search and LLM serving

A strong answer begins by fixing the workload and success criterion, then traces the request through embedding API, write log, vector store, ANN index. The governing quantitative model is: For vectors $x,y\in\mathbb{R}^d$, cosine similarity is $s(x,y)=\frac{x^\top y}{\lVert x\rVert_2\lVert y\rVert_2}$. A raw float32 collection needs approximately $4Nd$ bytes before graph edges, metadata, replicas, and allocator overhead. Exact scan is $O(Nd)$ per query; ANN trades exactness for a far smaller candidate set. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is exact brute-force k-nearest-neighbor search on a labeled sample; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ANN index** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Vector Databases, that boundary interacts with recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (exact brute-force k-nearest-neighbor search on a labeled sample), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: DiskANN: SSD-resident Vamana graph with beam search — I/O pattern analysis vs HNSW

```text
Vector Databases: DiskANN SSD resident Vamana graph with beam search I O pattern analysis vs HNSW

  [producer / request]
           |
           v
      [embedding API] ---- control, version, policy ----> [write log]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [vector store] ---- measured result + trace -----> [ANN index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **DiskANN: SSD-resident Vamana graph with beam search — I/O pattern analysis vs HNSW** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 2: Hybrid sparse-dense index: SPLADE sparse encoder + dense HNSW, unified scoring

```text
Vector Databases: Hybrid sparse dense index SPLADE sparse encoder dense HNSW unified scoring

  [producer / request]
           |
           v
      [write log] ---- control, version, policy ----> [vector store]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [ANN index] ---- measured result + trace -----> [metadata index]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Hybrid sparse-dense index: SPLADE sparse encoder + dense HNSW, unified scoring** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 3: Learned index: neural network as approximate KNN oracle — training pipeline + fallback strategy

```text
Vector Databases: Learned index neural network as approximate KNN oracle training pipeline fallback strategy

  [producer / request]
           |
           v
      [vector store] ---- control, version, policy ----> [ANN index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [metadata index] ---- measured result + trace -----> [query coordinator]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Learned index: neural network as approximate KNN oracle — training pipeline + fallback strategy** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

#### Diagram 4: Streaming HNSW: concurrent insert/delete with epoch-based garbage collection

```text
Vector Databases: Streaming HNSW concurrent insert delete with epoch based garbage collection

  [producer / request]
           |
           v
      [ANN index] ---- control, version, policy ----> [metadata index]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [query coordinator] ---- measured result + trace -----> [reranker]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Streaming HNSW: concurrent insert/delete with epoch-based garbage collection** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility.

### Case Studies

#### Microsoft DiskANN: how they serve 1B-vector search from commodity SSDs at 1ms latency — the Vamana graph construction algorithm vs HNSW construction complexity

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Microsoft DiskANN: how they serve 1B-vector search from commodity SSDs at 1ms latency — the Vamana graph construction algorithm vs HNSW construction complexity**—creates pressure at the **embedding API** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Qdrant's sparse vector support: implementing SPLADE-compatible sparse index alongside dense HNSW for hybrid search

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Qdrant's sparse vector support: implementing SPLADE-compatible sparse index alongside dense HNSW for hybrid search**—creates pressure at the **write log** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Google ScaNN: learned quantization via anisotropic quantization loss — how it outperforms standard PQ on inner product spaces

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Google ScaNN: learned quantization via anisotropic quantization loss — how it outperforms standard PQ on inner product spaces**—creates pressure at the **vector store** layer of Vector Databases. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use exact brute-force k-nearest-neighbor search on a labeled sample. Instrument it for recall, tail latency, memory, write freshness, filtering selectivity, and embedding-version compatibility, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

This frontier module has no fixed interview-question list. Turn each learning objective into a research-defense question: state the hypothesis, strongest alternative explanation, decisive experiment, and evidence that would make you abandon the idea.

## Hands-On Projects

### Beginner

- **Beginner project (proposed): Traceable Vector Databases slice.** Implement one request path using embedding API, write log, vector store. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Beginner project (proposed): Baseline-to-design evaluation.** Build exact brute-force k-nearest-neighbor search on a labeled sample, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Intermediate

- **Intermediate project (proposed): Traceable Vector Databases slice.** Implement one request path using embedding API, write log, vector store. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Intermediate project (proposed): Baseline-to-design evaluation.** Build exact brute-force k-nearest-neighbor search on a labeled sample, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Advanced

- **Advanced project (proposed): Traceable Vector Databases slice.** Implement one request path using embedding API, write log, vector store. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Advanced project (proposed): Baseline-to-design evaluation.** Build exact brute-force k-nearest-neighbor search on a labeled sample, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Research

- **Research project:** What are the fundamental limits of graph-based ANN search? Is there a theoretical lower bound on the index size / recall tradeoff?
- **Research project:** DiskANN beats HNSW on billion-scale benchmarks. Why doesn't everyone use it? What are its failure modes?
- **Research project:** How would you design a vector index that supports efficient deletion without index rebuild?
- **Research project:** Describe an open research problem in vector search that you find compelling and sketch a solution.

## Related Platform Content

- [Search Engines](/system-design/search-engines)
- [RAG Systems](/system-design/rag-systems)
- [Retrieval at Scale curriculum](/learn/ai-system-design/system-design-for-retrieval-at-scale)
- [HNSW architecture context](/architectures/graphsage)

## Further Reading

Prefer primary papers, standards, official engineering publications, and maintained documentation. Start with:

- Malkov and Yashunin, Efficient and Robust Approximate Nearest Neighbor Search Using HNSW (2018)
- Jégou, Douze, and Schmid, Product Quantization for Nearest Neighbor Search (2011)
- FAISS documentation and research papers

Use sources as evidence, not authority. Record the workload, date, hardware or data setting, and limitations behind every performance claim before transferring it to a different system.
