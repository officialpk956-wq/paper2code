# Recommendation Systems

A recommendation system chooses a small, ordered set of items for one user and context from a catalog that may contain millions of possibilities. Unlike search, the user may not state a query, so the system must infer intent while managing feedback loops and long-term welfare.

This article progresses from first principles to research questions. At every level, trace a request, state assumptions, quantify tradeoffs, and preserve a simple baseline. The recurring design pressures are candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured.

## Beginner: Foundations

:::note
**Prerequisites:** **Linear algebra: matrix operations, SVD conceptually**, **Basic probability: conditional probability, Bayes rule**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **build the vocabulary and trace one request end to end**. A recommendation system chooses a small, ordered set of items for one user and context from a catalog that may contain millions of possibilities. Unlike search, the user may not state a query, so the system must infer intent while managing feedback loops and long-term welfare. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Explain collaborative filtering, content-based filtering, and hybrid approaches

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **event log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Understand the cold-start problem and strategies for solving it

Cold start means interactions are insufficient for a new user or item. Use content features, onboarding preferences, contextual/popularity priors, and controlled exploration; then transition toward personalized signals as evidence accumulates. The capability described by this objective is practical, not a definition to memorize. Begin at the **feature platform** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Know what matrix factorization is and why it works for recommendation

Matrix factorization represents users and items with latent vectors and scores a pair with a dot product plus optional biases. Training moves observed positive pairs closer and sampled or explicit negatives apart, yielding efficient retrieval but limited context modeling. The capability described by this objective is practical, not a definition to memorize. Begin at the **candidate generators** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Describe a basic recommendation pipeline: candidate generation → scoring → serving

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ranking model** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the beginner level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: Recommendation pipeline: user history → candidate generation → scoring → filtering → ranking → UI

```text
Recommendation Systems: Recommendation pipeline user history candidate generation scoring filtering ranking UI

[user history]
        |
        v
  [candidate generation]
          |
          v
    [scoring]
            |
            v
      [filtering]
              |
              v
        [ranking]
                |
                v
          [UI]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Recommendation pipeline: user history → candidate generation → scoring → filtering → ranking → UI** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 2: User-item matrix: sparse binary interactions → matrix factorization → user/item embedding space

```text
Recommendation Systems: User item matrix sparse binary interactions matrix factorization user item embedding space

[sparse binary interactions]
        |
        v
  [matrix factorization]
          |
          v
    [user/item embedding space]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **User-item matrix: sparse binary interactions → matrix factorization → user/item embedding space** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 3: Cold-start decision tree: new user → demographics-based / trending → first interaction → collaborative filtering activates

```text
Recommendation Systems: Cold start decision tree new user demographics based trending first interaction collaborative filtering activates

[new user]
        |
        v
  [demographics-based / trending]
          |
          v
    [first interaction]
            |
            v
      [collaborative filtering activates]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Cold-start decision tree: new user → demographics-based / trending → first interaction → collaborative filtering activates** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 4: Content-based filter: item feature vector → similarity computation → top-N similar items

```text
Recommendation Systems: Content based filter item feature vector similarity computation top N similar items

[item feature vector]
        |
        v
  [similarity computation]
          |
          v
    [top-N similar items]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Content-based filter: item feature vector → similarity computation → top-N similar items** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

### Case Studies

#### Netflix Prize (2006–2009): what the winning solution (ensemble of 800+ models) revealed about the limits of matrix factorization alone

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Netflix Prize (2006–2009): what the winning solution (ensemble of 800+ models) revealed about the limits of matrix factorization alone**—creates pressure at the **event log** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Amazon "Customers who bought this also bought": the item-to-item collaborative filtering system that drove 35% of Amazon's revenue

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Amazon "Customers who bought this also bought": the item-to-item collaborative filtering system that drove 35% of Amazon's revenue**—creates pressure at the **feature platform** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### YouTube 2016 recommendation paper: the two-stage DNN candidate generation + ranking architecture that replaced matrix factorization

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**YouTube 2016 recommendation paper: the two-stage DNN candidate generation + ranking architecture that replaced matrix factorization**—creates pressure at the **candidate generators** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: What is the cold-start problem? Give 3 strategies to handle it.

Cold start means interactions are insufficient for a new user or item. Use content features, onboarding preferences, contextual/popularity priors, and controlled exploration; then transition toward personalized signals as evidence accumulates. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: Explain matrix factorization in 2 minutes.

Matrix factorization represents users and items with latent vectors and scores a pair with a dot product plus optional biases. Training moves observed positive pairs closer and sampled or explicit negatives apart, yielding efficient retrieval but limited context modeling. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: Why does collaborative filtering fail for niche items (the long tail problem)?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: What's the difference between explicit and implicit feedback?

Explicit feedback is a direct rating or preference; implicit feedback is behavior such as view, click, dwell, or purchase. Implicit data is abundant but confounds preference with exposure and interface position, so missing interaction is not a clean negative label. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Intermediate: Design Deep Dive

:::note
**Prerequisites:** **Beginner module complete**, **Deep learning: embeddings, feedforward networks**, **Distributed systems: key-value stores, feature stores**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **make design choices and quantify their tradeoffs**. A recommendation system chooses a small, ordered set of items for one user and context from a catalog that may contain millions of possibilities. Unlike search, the user may not state a query, so the system must infer intent while managing feedback loops and long-term welfare. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Design a two-tower neural recommendation system from data collection to serving

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **event log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Implement feature engineering for recommendations: user features, item features, context features

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **feature platform** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Reason about the explore/exploit tradeoff: epsilon-greedy, UCB, Thompson Sampling

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **candidate generators** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Design an offline evaluation protocol: precision@K, recall@K, NDCG@K, coverage, novelty

NDCG discounts relevant results that appear low in the ranking and normalizes by the best possible ordering for that query. It supports graded relevance, making it more informative than binary accuracy when position strongly affects user value. The capability described by this objective is practical, not a definition to memorize. Begin at the **ranking model** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the intermediate level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: Two-tower model: user tower (demographics + history → embedding) + item tower (content + metadata → embedding) → dot product → ANN retrieval

```text
Recommendation Systems: Two tower model user tower demographics history embedding item tower content metadata embedding dot product ANN retrieval

[user tower (demographics + history]
        |
        v
  [embedding) + item tower (content + metadata]
          |
          v
    [embedding)]
            |
            v
      [dot product]
              |
              v
        [ANN retrieval]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Two-tower model: user tower (demographics + history → embedding) + item tower (content + metadata → embedding) → dot product → ANN retrieval** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 2: Feature store architecture: raw events → stream processing → feature computation → online store (low latency) + offline store (training)

```text
Recommendation Systems: Feature store architecture raw events stream processing feature computation online store low latency offline store training

[raw events]
        |
        v
  [stream processing]
          |
          v
    [feature computation]
            |
            v
      [online store (low latency) + offline store (training)]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Feature store architecture: raw events → stream processing → feature computation → online store (low latency) + offline store (training)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 3: Explore-exploit bandit: A/B arm assignment → UCB score computation → item selection → reward observation → posterior update

```text
Recommendation Systems: Explore exploit bandit A B arm assignment UCB score computation item selection reward observation posterior update

[A/B arm assignment]
        |
        v
  [UCB score computation]
          |
          v
    [item selection]
            |
            v
      [reward observation]
              |
              v
        [posterior update]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Explore-exploit bandit: A/B arm assignment → UCB score computation → item selection → reward observation → posterior update** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 4: Recommendation feedback loop: served item → user interaction → reward signal → online learning update

```text
Recommendation Systems: Recommendation feedback loop served item user interaction reward signal online learning update

[served item]
        |
        v
  [user interaction]
          |
          v
    [reward signal]
            |
            v
      [online learning update]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Recommendation feedback loop: served item → user interaction → reward signal → online learning update** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 5: Multi-objective ranking: relevance score + diversity score + freshness score → weighted combination → Pareto front

```text
Recommendation Systems: Multi objective ranking relevance score diversity score freshness score weighted combination Pareto front

[relevance score + diversity score + freshness score]
        |
        v
  [weighted combination]
          |
          v
    [Pareto front]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Multi-objective ranking: relevance score + diversity score + freshness score → weighted combination → Pareto front** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

### Case Studies

#### Spotify Discover Weekly: how they combined collaborative filtering (Matrix Factorization on play history) + NLP (word2vec on playlist co-occurrence) + audio features — the three-signal architecture

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Spotify Discover Weekly: how they combined collaborative filtering (Matrix Factorization on play history) + NLP (word2vec on playlist co-occurrence) + audio features — the three-signal architecture**—creates pressure at the **event log** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Pinterest: Pinnability model — training a two-tower model on 1B+ interactions with 10B items, how they handle extreme long-tail

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Pinterest: Pinnability model — training a two-tower model on 1B+ interactions with 10B items, how they handle extreme long-tail**—creates pressure at the **feature platform** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Etsy: diversity in recommendations — how they penalize showing similar items from the same shop, the effect on user session length

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Etsy: diversity in recommendations — how they penalize showing similar items from the same shop, the effect on user session length**—creates pressure at the **candidate generators** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: Walk me through a two-tower recommendation model from raw logs to serving.

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: How do you handle the popularity bias problem — when your model always recommends popular items?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: Design a recommendation system for a new e-commerce site with zero historical data.

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: How would you detect if your recommendation model is causing a filter bubble?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q5: What's the difference between online and offline evaluation of a recommendation system?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Advanced: Production at Scale

:::note
**Prerequisites:** **Intermediate module complete**, **Sequence models: LSTM, Transformers**, **Causal inference basics: propensity scoring, inverse probability weighting**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **operate the system under scale, failure, security, and cost pressure**. A recommendation system chooses a small, ordered set of items for one user and context from a catalog that may contain millions of possibilities. Unlike search, the user may not state a query, so the system must infer intent while managing feedback loops and long-term welfare. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Design an industrial recommendation system handling 100M users, 1B items, 1M QPS

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **event log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Implement sequential recommendation: session-based models (GRU4Rec, SASRec, BERT4Rec)

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **feature platform** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Design a multi-task learning ranker: simultaneously optimize CTR, watch time, share rate, and not-interested rate

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **candidate generators** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Build a recommendation monitoring stack: drift detection, position bias correction, counterfactual evaluation

Items near the top are more likely to be seen regardless of relevance, so click labels contain both examination and preference. Correct with randomized interventions, inverse-propensity weighting, or models that explicitly estimate examination probability. The capability described by this objective is practical, not a definition to memorize. Begin at the **ranking model** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the advanced level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: Industrial recommendation stack: log collection → feature store → retrieval (multiple candidate generators in parallel) → L2 ranking → business rules filter → experiment layer → serving

```text
Recommendation Systems: Industrial recommendation stack log collection feature store retrieval multiple candidate generators in parallel L2 ranking business rules filter experiment layer serving

[log collection]
        |
        v
  [feature store]
          |
          v
    [retrieval (multiple candidate generators in parallel)]
            |
            v
      [L2 ranking]
              |
              v
        [business rules filter]
                |
                v
          [experiment layer]
                  |
                  v
            [serving]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Industrial recommendation stack: log collection → feature store → retrieval (multiple candidate generators in parallel) → L2 ranking → business rules filter → experiment layer → serving** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 2: SASRec / BERT4Rec: self-attention over item interaction sequence → next-item prediction → candidate generation

```text
Recommendation Systems: SASRec BERT4Rec self attention over item interaction sequence next item prediction candidate generation

[self-attention over item interaction sequence]
        |
        v
  [next-item prediction]
          |
          v
    [candidate generation]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **SASRec / BERT4Rec: self-attention over item interaction sequence → next-item prediction → candidate generation** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 3: Multi-task ranker: shared bottom tower + task-specific heads (CTR head, LTM head, share head) → Multi-gate Mixture of Experts (MMoE)

```text
Recommendation Systems: Multi task ranker shared bottom tower task specific heads CTR head LTM head share head Multi gate Mixture of Experts MMoE

[shared bottom tower + task-specific heads (CTR head, LTM head, share hea]
        |
        v
  [Multi-gate Mixture of Experts (MMoE)]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Multi-task ranker: shared bottom tower + task-specific heads (CTR head, LTM head, share head) → Multi-gate Mixture of Experts (MMoE)** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 4: Position bias correction: propensity-weighted loss function — position propensity model training → debiased ranker training

```text
Recommendation Systems: Position bias correction propensity weighted loss function position propensity model training debiased ranker training

[propensity-weighted loss function — position propensity model training]
        |
        v
  [debiased ranker training]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Position bias correction: propensity-weighted loss function — position propensity model training → debiased ranker training** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 5: Recommendation A/B test: orthogonal experiment layers (retrieval experiment / ranking experiment / UI experiment) — how Google's experimentation framework handles this

```text
Recommendation Systems: Recommendation A B test orthogonal experiment layers retrieval experiment ranking experiment UI experiment how Google s experimentation framework handles this

  [producer / request]
           |
           v
      [policy/diversity layer] ---- control, version, policy ----> [experiment service]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [feedback loop] ---- measured result + trace -----> [event log]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Recommendation A/B test: orthogonal experiment layers (retrieval experiment / ranking experiment / UI experiment) — how Google's experimentation framework handles this** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

### Case Studies

#### YouTube Deep Ranking (2019): multi-task ranking with 1B+ users — the Mixture of Experts architecture, how they decompose "satisfied" users from "engaged" users (watch time ≠ satisfaction)

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**YouTube Deep Ranking (2019): multi-task ranking with 1B+ users — the Mixture of Experts architecture, how they decompose "satisfied" users from "engaged" users (watch time ≠ satisfaction)**—creates pressure at the **event log** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### TikTok FYP (deep dive at this level): the dual-optimization of completion rate + like rate + follow rate + not-interested signals — how they weight them per user cohort

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**TikTok FYP (deep dive at this level): the dual-optimization of completion rate + like rate + follow rate + not-interested signals — how they weight them per user cohort**—creates pressure at the **feature platform** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Alibaba: real-time personalization with reinforcement learning — updating user embeddings in real time during a shopping session using recurrent models

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Alibaba: real-time personalization with reinforcement learning — updating user embeddings in real time during a shopping session using recurrent models**—creates pressure at the **candidate generators** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

#### Q1: Design YouTube's recommendation system. How do you generate candidates for 2.5B users with 800M videos?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q2: You notice your recommendation CTR is going up but user satisfaction scores are going down. What's happening and how do you fix it?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q3: How do you evaluate a recommendation system change when you can't run a proper A/B test (e.g., small user base, no control group)?

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q4: Walk me through how you'd implement real-time user modeling — updating user representations as they interact with the app.

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

#### Q5: What is position bias and how does it corrupt your training data?

Items near the top are more likely to be seen regardless of relevance, so click labels contain both examination and preference. Correct with randomized interventions, inverse-propensity weighting, or models that explicitly estimate examination probability. For **Recommendation Systems**, make the answer operational: draw the critical path, put a budget on each stage, and say what happens when a dependency is slow, stale, unauthorized, or unavailable. Distinguish measured facts from assumptions and avoid invented company internals.

Then close with validation. Use an offline quality set for repeatability, a load or failure test for system behavior, and a guarded online rollout for user impact. Track an end-to-end outcome plus the stage-level metrics needed to localize regressions. This structure answers both halves of a system-design interview: why the mechanism works and how you would operate it responsibly.

## Research: Frontiers

:::note
**Prerequisites:** **Advanced module complete**, **Read: SASRec (2018), BERT4Rec (2019), P5 (2022), LLMRank (2023), RecSys survey (He 2017)**. Before continuing, explain each item in your own words and identify where it appears in the request path below.
:::

At this level, the goal is to **identify open assumptions, evaluate frontier ideas, and design falsifiable experiments**. A recommendation system chooses a small, ordered set of items for one user and context from a catalog that may contain millions of possibilities. Unlike search, the user may not state a query, so the system must infer intent while managing feedback loops and long-term welfare. Keep one principle visible: a system is not the boxes in its diagram. It is the contracts, queues, ownership boundaries, measurements, and recovery behavior between those boxes.

#### Objective 1: Analyze the frontier: contrastive self-supervised recommendation, causal recommendation, LLM-augmented recommendation

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **event log** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 2: Design a research experiment testing a specific hypothesis about user behavior modeling

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **feature platform** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 3: Critique current evaluation protocols and propose improvements

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **candidate generators** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

#### Objective 4: Read and synthesize 5 papers on sequential recommendation

A strong answer begins by fixing the workload and success criterion, then traces the request through event log, feature platform, candidate generators, ranking model. The governing quantitative model is: A two-tower retriever often uses $s(u,i)=e_u^\top e_i$, while the ranker estimates $\hat y=f(e_u,e_i,x_{ui})$. For implicit feedback, the logged label is exposure-dependent: $P(\text{click})=P(\text{examined})P(\text{click}\mid\text{examined})$, which is why position bias must be measured. Use it to estimate an order of magnitude before selecting infrastructure. Then quantify candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, state which data is authoritative, and name the failure and fallback paths. The simplest credible baseline is popular and recently popular items evaluated alongside a simple matrix-factorization model; compare against it before adding complexity. The capability described by this objective is practical, not a definition to memorize. Begin at the **ranking model** boundary and trace the data entering it, the state it reads, the output contract, and the owner of failures. In Recommendation Systems, that boundary interacts with candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction. A design is incomplete if it names a component but cannot say how it is measured, versioned, isolated, and recovered.

At the research level, the useful question is: *what decision can you make after learning this?* Write assumptions before selecting technology. Use a representative workload, identify the cheapest credible baseline (popular and recently popular items evaluated alongside a simple matrix-factorization model), and measure quality and operational cost together. Separate control-plane work—configuration, policy, placement, and metadata—from data-plane work on the live request. This prevents slow management operations from entering the latency-critical path. Finally, test one normal case, one boundary case, and one dependency failure. That small discipline turns the objective into an engineering skill and exposes where an apparently elegant diagram hides queues, stale state, or unsafe fallback behavior.

### Diagrams

#### Diagram 1: P5: pretraining 5 recommendation tasks with a T5 model as a unified text-to-text framework

```text
Recommendation Systems: P5 pretraining 5 recommendation tasks with a T5 model as a unified text to text framework

  [producer / request]
           |
           v
      [event log] ---- control, version, policy ----> [feature platform]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [candidate generators] ---- measured result + trace -----> [ranking model]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **P5: pretraining 5 recommendation tasks with a T5 model as a unified text-to-text framework** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 2: LLM as ranker: in-context learning with user history as context → LLM → ranked item list — analysis of why LLMs struggle with popularity bias

```text
Recommendation Systems: LLM as ranker in context learning with user history as context LLM ranked item list analysis of why LLMs struggle with popularity bias

[in-context learning with user history as context]
        |
        v
  [LLM]
          |
          v
    [ranked item list — analysis of why LLMs struggle with popularity bias]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **LLM as ranker: in-context learning with user history as context → LLM → ranked item list — analysis of why LLMs struggle with popularity bias** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 3: Causal recommendation: treatment (recommendation exposure) / outcome (conversion) / confounder (user intent) — do-calculus diagram for debiased recommendation

```text
Recommendation Systems: Causal recommendation treatment recommendation exposure outcome conversion confounder user intent do calculus diagram for debiased recommendation

  [producer / request]
           |
           v
      [candidate generators] ---- control, version, policy ----> [ranking model]
           |                                         |
           | data / candidate / event                | state / cache
           v                                         v
      [policy/diversity layer] ---- measured result + trace -----> [experiment service]
           |                                         |
           +-------- retry / fallback / audit <------+
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Causal recommendation: treatment (recommendation exposure) / outcome (conversion) / confounder (user intent) — do-calculus diagram for debiased recommendation** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

#### Diagram 4: Contrastive self-supervised pre-training: item sequence → augmented views → contrastive loss → pre-trained encoder → fine-tuned recommendation model

```text
Recommendation Systems: Contrastive self supervised pre training item sequence augmented views contrastive loss pre trained encoder fine tuned recommendation model

[item sequence]
        |
        v
  [augmented views]
          |
          v
    [contrastive loss]
            |
            v
      [pre-trained encoder]
              |
              v
        [fine-tuned recommendation model]
```

Read the diagram from left to right for the normal path and from the bottom back toward the top for recovery. The label **Contrastive self-supervised pre-training: item sequence → augmented views → contrastive loss → pre-trained encoder → fine-tuned recommendation model** determines what each arrow carries; record its schema, freshness requirement, and failure behavior. In an interview, redraw this small path first, then add partitions, replicas, queues, and security boundaries only when the requirements demand them. That order keeps the explanation understandable while still exposing candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction.

### Case Studies

#### Meta's DLRM at 1T parameters: embedding tables too large for GPU memory — the hybrid CPU/GPU pipeline, gradient compression for embedding tables

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Meta's DLRM at 1T parameters: embedding tables too large for GPU memory — the hybrid CPU/GPU pipeline, gradient compression for embedding tables**—creates pressure at the **event log** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### Google's "Are We Really Making Much Progress? Revisiting, Benchmarking, and Refining Heterogeneous Graph Neural Networks" — the case that GNN-based recommendation doesn't consistently outperform simpler baselines

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**Google's "Are We Really Making Much Progress? Revisiting, Benchmarking, and Refining Heterogeneous Graph Neural Networks" — the case that GNN-based recommendation doesn't consistently outperform simpler baselines**—creates pressure at the **feature platform** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

#### RecSys reproducibility crisis: papers that claimed improvements on MovieLens but couldn't be reproduced — what went wrong in evaluation

Treat this case as a design investigation rather than a claim that every internal detail is public. The stated scenario—**RecSys reproducibility crisis: papers that claimed improvements on MovieLens but couldn't be reproduced — what went wrong in evaluation**—creates pressure at the **candidate generators** layer of Recommendation Systems. Start by writing the actor, workload, user-visible success condition, and failure cost. Preserve any numbers given in the prompt as requirements; where numbers are absent, label estimates explicitly rather than presenting them as company facts.

The first design should use popular and recently popular items evaluated alongside a simple matrix-factorization model. Instrument it for candidate recall, ranking quality, freshness, diversity, fairness, exploration, and long-term satisfaction, then locate the first bottleneck with evidence. Introduce one mechanism at a time: partitioning for capacity, caching for repeated work, asynchronous processing for non-critical tasks, and a fallback for dependency failure. Keep authorization and tenant boundaries on the data path rather than filtering after retrieval or generation. For rollout, shadow real traffic, compare against the baseline, inspect important slices, and define a rollback threshold before exposure.

The lesson is the decision process. A successful case study explains why one resource became scarce, why the chosen mechanism addresses that scarcity, and what new failure mode it creates. It closes with a measurement plan: one quality metric, one tail-latency or freshness metric, one cost metric, and one safety or correctness invariant. That makes the story reusable instead of a list of brand-name components.

### Interview Questions & Answers

This frontier module has no fixed interview-question list. Turn each learning objective into a research-defense question: state the hypothesis, strongest alternative explanation, decisive experiment, and evidence that would make you abandon the idea.

## Hands-On Projects

### Beginner

- **Beginner project (proposed): Traceable Recommendation Systems slice.** Implement one request path using event log, feature platform, candidate generators. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Beginner project (proposed): Baseline-to-design evaluation.** Build popular and recently popular items evaluated alongside a simple matrix-factorization model, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Intermediate

- **Intermediate project (proposed): Traceable Recommendation Systems slice.** Implement one request path using event log, feature platform, candidate generators. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Intermediate project (proposed): Baseline-to-design evaluation.** Build popular and recently popular items evaluated alongside a simple matrix-factorization model, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Advanced

- **Advanced project (proposed): Traceable Recommendation Systems slice.** Implement one request path using event log, feature platform, candidate generators. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Advanced project (proposed): Baseline-to-design evaluation.** Build popular and recently popular items evaluated alongside a simple matrix-factorization model, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

### Research

- **Research project (proposed): Traceable Recommendation Systems slice.** Implement one request path using event log, feature platform, candidate generators. Log correlation IDs and stage latency, inject one dependency failure, and demonstrate a safe fallback. Deliver a diagram, runnable code, load-test result, and one-page decision record.
- **Research project (proposed): Baseline-to-design evaluation.** Build popular and recently popular items evaluated alongside a simple matrix-factorization model, add one mechanism from this module, and compare quality, p95/p99 latency, resource cost, and a safety or correctness invariant. Include assumptions, raw results, failure examples, and a rollback threshold.

## Related Platform Content

- [YouTube Architecture](/system-design/youtube-architecture)
- [Netflix Architecture](/system-design/netflix-architecture)
- [TikTok Architecture](/system-design/tiktok-architecture)
- [Matrix Factorization](/architectures/matrix-factorization)

## Further Reading

Prefer primary papers, standards, official engineering publications, and maintained documentation. Start with:

- Covington, Adams, and Sargin, Deep Neural Networks for YouTube Recommendations (2016)
- Koren, Bell, and Volinsky, Matrix Factorization Techniques for Recommender Systems (2009)
- Recommender Systems Handbook

Use sources as evidence, not authority. Record the workload, date, hardware or data setting, and limitations behind every performance claim before transferring it to a different system.
