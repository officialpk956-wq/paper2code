# Implement Matrix Multiplication

Implement `matmul(A, B)` from scratch — no `np.dot`, no `@`. Then explain its time complexity.

## Problem Statement

Given matrix `A` of shape `(m, n)` and matrix `B` of shape `(n, p)`, return their product `C` of shape `(m, p)` where:

$$
C_{ij} = \sum_{k=1}^{n} A_{ik} B_{kj}
$$

Raise a `ValueError` if the inner dimensions don't match.

## Starter Code

```python
def matmul(A: list[list[float]], B: list[list[float]]) -> list[list[float]]:
    """Multiply two matrices represented as nested lists.

    Raises:
        ValueError: if A's column count != B's row count.
    """
    # Your implementation here
    ...
```

## Examples

```python
A = [[1, 2],
     [3, 4]]
B = [[5, 6],
     [7, 8]]

matmul(A, B)
# [[19, 22],
#  [43, 50]]
```

## Constraints

- `1 <= m, n, p <= 200`
- Pure Python (lists), no NumPy for the core implementation

:::tip
Stuck? C[i][j] is just a dot product. Write a `dot(row, col)` helper first and the rest becomes two loops.
:::

## Follow-Up Questions

1. What is the time complexity? Why is this the bottleneck of deep learning?
2. Why is `A @ B` in NumPy thousands of times faster than your version? (Hint: BLAS, cache locality, SIMD.)
3. In a Transformer layer with `d_model = 4096`, how many multiply-adds does a single linear projection of one token cost?
