<USER_REQUEST>
# Paper2Code — Phase 19: Backend Implementation

You are Antigravity implementing the full backend for Paper2Code.

Visual reference: Replit/Stitch prototype project ID 13282654367646092033
(open it for exact layout reference for every page you wire up)

Read these existing files FIRST before writing a single line:
  src/data/problems.ts          — Problem type already has pythonTemplate, testCases, hints
  src/app/api/dojo/run/route.ts — Code execution API already exists
  src/app/api/dojo/submit/route.ts
  src/lib/progress/index.ts     — Progress tracking (localStorage)
  src/lib/persistence/index.ts  — load<T>/save helpers
  src/lib/assessment/index.ts   — Profile, scoring
  src/lib/recommendations/index.ts
  src/lib/content/loader.ts     — getAllSlugs, getPaper, getAllContent
  src/lib/roadmap-intelligence/index.ts
  src/lib/study-sessions/index.ts
  src/lib/dojo/index.ts
  src/data/domains/             — all domain files

DO NOT duplicate logic that already exists. Wire what's there. Fill gaps only.

═══════════════════════════════════════════
STEP 0 — FIX CHUNK LOADING ERRORS (do first, block everything else on this)
═══════════════════════════════════════════

Routes currently broken with "Loading chunk failed":
  /learn, /dojo, /dashboard

Diagnose:
  1. Run: npx tsc --noEmit 2>&1 | head -80
     Fix all TypeScript errors before anything else. Zero tolerance.

  2. Check for circular imports in recently added files:
     src/lib/assessment/path.ts imports from src/lib/progress + src/data/topics
     src/hooks/use-user-stats.ts
     src/data/topics/index.ts (recently extended)
     Any file importing from src/lib/assessment that is also imported by it

  3. Check that every component imported by /learn/page.tsx, /dojo/page.tsx,
     /dashboard/page.tsx either:
       a) Has 'use client' if it uses hooks/b
<truncated 27252 bytes>
i/dojo/submit
        □ Test cases tab shows visible cases from problem data
        □ ← Problems navigates back

STEP 2: □ getContributionGrid(52) returns 364 items □ No Date.now() calls

STEP 3: □ Activity calendar renders on /dashboard □ All squares visible
        □ Stats show real 0 values (not hardcoded) □ Profile card loads bio from localStorage

STEP 4: □ /learn page shows accordion sections
        □ Clicking ML section expands with paper timeline
        □ Paper links resolve via getIndexEntry

STEP 5: □ /interview renders question list
        □ Expanding question shows key points + sample answer
        □ Practice answer saves to localStorage

STEP 6: □ /playground loads
        □ Cost calculator computes live on slider change
        □ FLOPs calculator computes live on dimension input
        □ Code sandbox runs Python via /api/dojo/run

STEP 7: □ /feedback renders form □ Submit saves to localStorage
        □ Success state shows after submit

STEP 8: □ /study-plan renders 4 plan cards
        □ Selecting a plan shows phase accordion
        □ Checking a resource marks it complete in localStorage

FINAL:  □ npx vitest run → ≥578 tests pass
        □ npx tsc --noEmit → 0 errors
        □ All routes load without chunk errors
        □ grep -r "hardcoded\|progress=3[0-9]\|\"34\"" src/ → 0 results
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-23T23:46:39+05:30.

The user's current state is as follows:
Active Document: c:\Users\BIT\.gemini\config\mcp_config.json (LANGUAGE_JSON)
Cursor is on line: 1
Other open documents:
- c:\Users\BIT\.gemini\config\mcp_config.json (LANGUAGE_JSON)
- c:\papper2code\.git\COMMIT_EDITMSG (LANGUAGE_UNSPECIFIED)
- c:\papper2code\src\components\dojo\DojoEditor.tsx (LANGUAGE_TSX)
Browser State:
  Page 30445A4E8C7E05EF5C9B550898DA1C9F (Paper2Code — AI Engineering Learning Ecosystem) - http://localhost:3000/dashboard [ACTIVE]
    Viewport: 1280x585, Page Height: 585
</ADDITIONAL_METADATA>