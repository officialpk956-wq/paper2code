{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ':(exclude)backend' ':(exclude)core' ':(exclude)requirements.txt' ':(exclude)Dockerfile'"
}
