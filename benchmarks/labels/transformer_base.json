{
  "schema_version": 1,
  "paper_id": "transformer_base",
  "source": "arxiv:1706.03762",
  "family": "transformer",
  "expected": {
    "layer_types": [
      "multiheadattention",
      "feedforward",
      "layernorm",
      "linear",
      "positionalembedding",
      "embedding",
      "softmax",
      "dropout"
    ],
    "key_hyperparams": {
      "hidden_size": 512,
      "num_heads": 8,
      "num_layers": 6
    },
    "has_residual": true
  },
  "notes": "The base Transformer uses model dimension 512, eight attention heads, and six encoder and decoder layers. Neutral pass 2026-09-19 (paper was silently skipped on 2026-09-17: its cached PDF had been removed from the scratchpad) -- positionalembedding: \"3.5 Positional Encoding: ... we add positional encodings to the input embeddings\"; embedding: \"we use learned embeddings to convert the input tokens and output tokens to vectors of dimension d_model\"; softmax: \"the usual learned linear transformation and softmax function to convert the decoder output to predicted next-token probabilities\"; dropout: \"Residual Dropout: We apply dropout to the output of each sub-layer\"."
}